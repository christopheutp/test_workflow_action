# Démo API Spring Boot — TDD et BDD

## Endpoints disponibles

- `POST /api/demo-items` : créer un élément.
- `GET /api/demo-items/{id}` : récupérer un élément.
- `GET /api/demo-items` : lister les éléments.
- `DELETE /api/demo-items` : vider le dépôt en mémoire.
